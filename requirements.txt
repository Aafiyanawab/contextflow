# Direct dependencies — pinned; audit against OSV before bumping.
openai==2.44.0
python-dotenv==1.2.2
flask==3.1.3
flask-sqlalchemy==3.1.1
authlib==1.7.2
pygithub==2.9.1
flask-migrate==4.1.0
pypdf==6.14.2
python-docx==1.2.0
numpy==2.2.6
tiktoken==0.13.0

# Deployment (Phase 0 — containerization):
#   gunicorn        production WSGI server (replaces Flask's dev server)
#   psycopg2-binary PostgreSQL driver (SQLAlchemy talks to Postgres through it)
#   boto3           AWS S3 client (prod uploads; MinIO stands in for S3 locally)
gunicorn==23.0.0
psycopg2-binary==2.9.10
boto3==1.40.47

# Observability (Prometheus): client for the /metrics endpoint. Pure-Python,
# no native deps — negligible footprint.
prometheus-client==0.23.1

# Transitive security floors (OSV audit 2026-07-06): versions below
# these carry known CVEs (decompression bombs, header leaks, OpenSSL).
cryptography>=49.0.0
requests>=2.34.2
urllib3>=2.7.0
